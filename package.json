{
  "name": "healthystock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:dev-env": "node scripts/setup-dev-env.mjs",
    "dev:docker": "npm run setup:dev-env && docker compose up -d --build",
    "setup:prod-env": "node scripts/setup-prod-env.mjs"
  }
}
